server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/udemy_project
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect